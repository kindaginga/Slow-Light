// Nothing bridged yet. The web app needs no Node APIs; this file exists so the
// sandbox is set up correctly and there is a place to add native features later
// (e.g. a native encoder or camera-roll save on mobile).
